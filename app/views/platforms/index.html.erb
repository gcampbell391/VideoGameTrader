<h1> All Platforms </h1>
<%= button_to 'Home', "/users/#{session[:user_id]}", method: :get %>
<ul>
<% @platforms.each do |platform| %>
<h2><%= link_to platform.name, "/platforms/#{platform.id}"%></h2>
<li>Released: <%= platform.release_year %></li>

<%end%>
</ul>
